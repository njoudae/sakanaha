import type { Property } from "@saknaha/shared-types";

/**
 * Legacy compatibility export. Production and local experiences now start
 * without seeded property records.
 */
export const mockProperties: Property[] = [];
